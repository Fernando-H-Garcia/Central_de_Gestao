# Fluxo da Aplicação

1. Inicialização (config → DB → migrations)
2. Tela principal carregada
3. Navegação entre módulos via sidebar
4. Operações CRUD via services
5. EventBus para comunicação entre views
6. Snapshot worker para cache de dados
